"use client";

// import styles from "./page.module.css";
import { useState } from "react";

const Dashboard = () => {
  const [active, setActive] = useState("");
  const [toggle, setToggle] = useState(false);

  return <div>On page</div>;
};

export default Dashboard;
